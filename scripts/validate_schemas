from xmlschema import XMLSchema

import sys

xml_schema = XMLSchema.meta_schema
xml_schema.build()

for file in sys.argv[1:]:

    print(f"Validating {file}")
    for errors in xml_schema.iter_errors(file):
        print(errors)
    print(xml_schema.is_valid(file))
